machine:
    timezone: Asia/Tokyo
    services:
        - docker

compile:
    override:
        - docker build -t u6kapps/blog-dev -f Dockerfile-env .
        - docker run --rm -v $(pwd):/var/my-blog blog-dev
        - docker build -t u6kapps/blog .

test:
    override:
        - echo test

deployment:
    release:
        tag: /.+/
        commands:
            - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
            - docker tag u6kapps/blog u6kapps/blog:$CIRCLE_TAG
            - docker push u6kapps/blog
