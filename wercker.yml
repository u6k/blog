box: u6kapps/jekyll-docker

build:
    steps:
        - bundle-install
        - script:
            name: site generate
            code: bundle exec jekyll build

deploy:
    steps:
        - script:
            name: site upload
            code: s3_website push
